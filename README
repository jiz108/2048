Jiawei Zhou
cs8bffb

Short Response:

1
type in:
:%s/fun/amazing/g
:%s/fun/amazing

2
type in:
mkdir -pfooDir/barDir

3
As we want to compile all the java file, we use javac *.java
in this case * is a wildcard character

4
type in
gvim -p *.java

5
this is a kind of method which can be assessed without instantiating the class which it belongs. For example, the main method, we just call it.

6
use parameter. It will be easier to just write method: drawCricle(color c).
It will save more time.
